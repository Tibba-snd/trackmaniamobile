# Tasks for Task 4: Racing Motion and Animations

- [x] Add fast skew-based entry animations (`streak-in-left`, `streak-in-right`, `streak-in-bottom-center`) to HUD boxes on race load
- [x] Implement odometer-style vertical roll click animation (`digit-change`) for speed and timer changes
- [x] Wire RPM-synced breathing border glow (`--rpm-glow-opacity`) onto `#hudSpeedBox` in the game loop
- [x] Trigger dramatic purple-sector flash (`purple-flash`) on `#hudLeftBox` upon crossing a checkpoint ahead of the PB ghost
- [x] Add dramatic "FINAL SECTOR" center alert animation on crossing the final checkpoint ahead of PB
- [x] Add PB celebration screen overlay flash and card border glow animation (`pb-celebration`) on new personal best
- [x] Sync changes using `apk-build/sync.bat` and verify functionality
